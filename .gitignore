node_modules/
dist/
coverage/
# Downloaded IPAs and other build output. Never commit a binary to this repo —
# it is public and permanent, and an IPA is ~14 MB.
build/
builds/
.expo/
.DS_Store
*.log
.env
.env.*
!.env.example
!.env.production.example
supabase/.temp/
supabase/.branches/

# Private working files — the repo is public and permanent, so these must
# never be committed.
HANDOFF.md
Bukit Pennies Codebase Study Plan.md
Security check reddit post.txt
