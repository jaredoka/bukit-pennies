node_modules/
dist/
coverage/
# Downloaded IPAs and other build output. Never commit a binary to this repo —
# it is public and permanent (HANDOFF §19), and an IPA is ~14 MB.
build/
builds/
.expo/
.DS_Store
*.log
.env
.env.*
!.env.example
!.env.production.example
supabase/.temp/
supabase/.branches/

# Private: personal study notes — the repo is public and permanent (HANDOFF §19),
# so these must never be committed.
Bukit Pennies Codebase Study Plan.md
Security check reddit post.txt
